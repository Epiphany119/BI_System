# 智能 BI 项目建设记录

## 当前版本：分析工作台增强版

### 已完成产出

- 增加“分析工作台”首页，展示分析总数、已完成、处理中、失败任务等核心指标。
- 增加最近分析列表，可快速跳转到图表管理、同步分析和异步分析。
- 增加快捷操作区，缩短从登录到创建分析的路径。
- 增加任务状态颜色标识，初步呈现等待、处理中、成功、失败四类状态。
- 将“我的图表”升级为管理页面，增加状态筛选、详情弹窗和删除确认。
- 增加图表分析结论详情展示，失败任务保留执行错误信息，便于定位问题。
- 保留并串联 Excel/CSV 上传、AI 分析结论、ECharts 图表、RabbitMQ 异步任务、Redis 限流等核心能力。
- 本地隐私配置统一收敛到 `yubi-backend-master/src/main/resources/application-local.yml`，并通过 `application.yml` 自动加载，且不会提交到 Git。

### 当前业务链路

1. 用户登录后进入分析工作台。
2. 用户上传 CSV/Excel 文件并填写分析目标。
3. 系统调用 AI 生成结构化分析结论和 ECharts 配置。
4. 耗时任务进入 RabbitMQ 异步队列，图表状态实时落库。
5. 用户在“我的图表”中查看成功结果、失败原因和历史记录。

### 下一阶段计划

- 图表管理增加编辑名称、删除、重试、详情抽屉和导出 PNG/Excel。
- 增加数据集管理：文件预览、字段统计、数据校验和复用数据集。
- 增加任务中心：进度、失败重试、超时处理、任务筛选。
- 增加分析详情：核心结论、趋势、异常点、关键指标和业务建议分区展示。
- 增加 Docker Compose、健康检查、接口测试、消息重试和操作审计日志。

### 简历可描述产出

> 构建基于 React、Spring Boot、MySQL、Redis、RabbitMQ 和大模型 API 的智能 BI 分析平台，支持用户上传 Excel/CSV 数据，自动生成结构化分析结论及 ECharts 可视化图表；通过 RabbitMQ 解耦耗时 AI 任务，并使用 Redis 实现用户级限流和分布式会话管理。

> 当前已完成核心分析链路和工作台，后续持续补充任务重试、数据集管理、导出和可观测性能力。
